# Runbook

Stub. To be filled in during Phase 1 (Constitution §6) once Sveltia CMS is wired up and
a second EC member has deploy access — covering how to edit content, deploy, and recover
from common failures.

For now, see `README.md` for install/dev/build/deploy commands and
`docs/CONSTITUTION.md` for everything else.
