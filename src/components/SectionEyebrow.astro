---
interface Props {
  label: string;
}
const { label } = Astro.props;
---

<div class="mb-4">
  <p class="font-mono text-xs uppercase tracking-widest text-powai">{label}</p>
  <div class="mt-2 h-px w-12 bg-crest" role="presentation"></div>
</div>
